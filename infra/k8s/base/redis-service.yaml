apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: exam-platform
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
  - name: redis
    port: 6379
    targetPort: redis
    protocol: TCP
  selector:
    app: redis
