apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: exam-platform
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: frontend
