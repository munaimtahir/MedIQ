apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: exam-platform
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
  selector:
    app: backend
